# ─── MONGODB CONFIGURATION ─────────────────────────────────────────────────────
# Get your free MongoDB URI from: https://cloud.mongodb.com
# Click "Connect" → "Connect your application" → copy the connection string
MONGODB_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net/foodscan
MONGODB_DB_NAME=foodscan

# ─── JWT CONFIGURATION ───────────────────────────────────────────────────────
# Generate a strong secret key (32+ characters recommended)
# Use: openssl rand -base64 32 or https://generate-secret.vercel.app/32
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# ─── GEMINI AI CONFIGURATION ───────────────────────────────────────────────────
# Get your API key from: https://makersuite.google.com/app/apikey
EXPO_PUBLIC_GEMINI_API_KEY=your-gemini-api-key-here

# ─── API CONFIGURATION ───────────────────────────────────────────────────────
# Your server URL (change if deploying to production)
EXPO_PUBLIC_API_URL=http://localhost:3001

# ─── DEVELOPMENT CONFIGURATION ───────────────────────────────────────────────
# Replit development (if using Replit)
REPLIT_DEV_DOMAIN=your-repl-domain.replit.app
REPLIT_DOMAINS=your-repl-domain.replit.app
